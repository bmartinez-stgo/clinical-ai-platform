apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocr-engine.fullname" . }}
  labels:
    {{- include "ocr-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ocr-engine.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
