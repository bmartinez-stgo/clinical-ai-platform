apiVersion: v1
kind: Service
metadata:
  name: {{ include "clinical-chat.fullname" . }}
  labels:
    {{- include "clinical-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "clinical-chat.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
